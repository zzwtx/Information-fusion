# 智能信息融合课程期末项目方案

## —— 基于 FedMGP 的跨模态提示联邦微调方法复现与改进

---

## 一、选题背景与动机

### 1.1 问题背景

视觉-语言预训练大模型（如 CLIP）在下游图像分类任务上展现出强大的零样本和少样本能力。然而，在实际联邦学习场景中，多个客户端的数据分布在不同的私有数据孤岛上，如何高效地协作微调 CLIP 而不泄露原始数据，成为一项关键挑战。

现有工作主要分为两类：
- **基于文本提示的联邦微调**：只训练文本端的可学习 prompt token，通信量小，但忽略了视觉端特征的适配。
- **基于视觉适配器的联邦微调**：只训练轻量级视觉 adapter，但失去了文本端的灵活性。

如何在联邦场景下同时利用文本提示和视觉提示的互补优势，同时保持低通信量，是一个开放问题。

### 1.2 选题依据

从 ICLR/ICML/NeurIPS 2024-2026 的联邦微调多模态大模型论文中，筛选出 3 篇与"跨模态提示融合"高度相关的论文：

| 论文 | 会议 | 核心方法 | 通信参数量 | 跨模态融合 |
|------|------|---------|-----------|-----------|
| **FedMGP** | NeurIPS 2025 | 多组文本-视觉提示 + 多样性损失 + 动态聚合 | 5.1K | 文本+视觉双提示 |
| pFedMMA | ICLR 2026 | 多模态 Adapter + 非对称聚合 | 3.0K | 文本/视觉 Adapter 并行 |
| FedDDA | ICML 2025 | 文本先验解耦 + 视觉动态适配 + MoE | 较大 | 文本引导视觉适配器 |

**选择 FedMGP 作为复现基准的原因**：

1. **跨模态融合特性最强**：FedMGP 同时学习文本提示组和视觉提示组，并通过多样性损失显式鼓励不同提示组捕获互补的跨模态特征，与课程"智能信息融合"中跨模态融合的主题最为契合。

2. **方法设计清晰、可复现性高**：FedMGP 整体架构为"多组提示生成 → 余弦相似度分类 → 多样性正则化 → 相似度引导动态聚合"，各组件解耦良好，适合后续进行模块化消融和改进。

3. **理论支撑完善**：论文给出了动态聚合的信噪比（SNR）理论分析（Theorem 1: SNR_full ≤ SNR_fixed < SNR_dyn），为聚合策略的合理性提供了形式化保证，这在三项工作中是独有的。

4. **实验覆盖全面但仍有扩展空间**：原文在 9 个 CLIP 数据集上验证，但编码器架构固定为 ViT-B/16、数据异质性分布仅考察了 α=0.5 的 Dirichlet、客户端数量固定在 10 个，这些都为补充实验留下了空间。

---

## 二、FedMGP 方法概述

### 2.1 核心思想

FedMGP（Personalized Federated Learning with Multi-Group Text-Visual Prompts）在联邦学习框架下，为每个客户端学习 $G$ 组文本-视觉提示对 $\{(\mathbf{P}_t^g, \mathbf{P}_v^g)\}_{g=1}^G$。每个提示组独立进行 CLIP 的余弦相似度分类，最终通过集成所有组的预测结果获得更鲁棒的分类决策。

### 2.2 方法框架

```
客户端 k:
  输入图像 → CLIP图像编码器(frozen) → 图像特征 f(x)
                                         ↓
  [可学习视觉提示P_v^1] [可学习视觉提示P_v^2] ... [P_v^G]
         ↓                      ↓                      ↓
    视觉特征v^1             视觉特征v^2          视觉特征v^G
         ↓                      ↓                      ↓
         └──────────────────────┼──────────────────────┘
                                ↓
  [可学习文本提示P_t^1] [可学习文本提示P_t^2] ... [P_t^G]
         ↓                      ↓                      ↓
    文本特征t^1             文本特征t^2          文本特征t^G
         ↓                      ↓                      ↓
    cos(v^1,t^1)           cos(v^2,t^2)         cos(v^G,t^G)
         ↓                      ↓                      ↓
         └──────────────────────┼──────────────────────┘
                                ↓
                    集成预测 (平均/加权)
                                ↓
                    + 多样性损失 L_div
```

### 2.3 关键公式

**预测**：对于第 $g$ 组提示，类别 $c$ 的预测概率为：
$$p_g(y=c|x) = \frac{\exp(\text{sim}(f(x) + \mathbf{P}_v^g, g(t_c) + \mathbf{P}_t^g)/\tau)}{\sum_{j=1}^C \exp(\text{sim}(f(x) + \mathbf{P}_v^g, g(t_j) + \mathbf{P}_t^g)/\tau)}$$

其中 $f(\cdot)$ 为冻结的 CLIP 图像编码器，$g(\cdot)$ 为冻结的 CLIP 文本编码器，$t_c$ 为类别 $c$ 的文本模板（如 "a photo of a [CLASS]"）。

**分类损失**（交叉熵）：
$$\mathcal{L}_{\text{CE}} = -\frac{1}{G}\sum_{g=1}^G\sum_{c=1}^C y_c \log p_g(y=c|x)$$

**多样性损失**（鼓励不同提示组产生差异化特征）：
$$\mathcal{L}_{\text{div}} = \frac{2}{G(G-1)}\sum_{i=1}^{G-1}\sum_{j=i+1}^G \left|\cos(\mathbf{f}^i, \mathbf{f}^j)\right|$$

其中 $\mathbf{f}^g$ 是第 $g$ 组提示对应的文本-视觉联合特征表示。

**总训练损失**：
$$\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{CE}} + \lambda \cdot \mathcal{L}_{\text{div}}$$

**相似度引导的动态聚合**（服务端）：
$$w_k = \text{softmax}(\cos(\mathbf{P}_k, \bar{\mathbf{P}}) / T)$$

$$\bar{\mathbf{P}}^{(t+1)} = \sum_{k \in \mathcal{S}_t} w_k \mathbf{P}_k^{(t)}$$

其中 $\mathcal{S}_t$ 为基于相似度选择的 top-$s$ 个客户端。该聚合方式在理论上保证 $\text{SNR}_{\text{dyn}} > \text{SNR}_{\text{fixed}}$（定理 1）。

### 2.4 核心技术特点

- **通信效率极高**：仅需传输约 5.1K 个参数（$G$ 组文本+视觉提示向量），远小于完整模型或 adapter 方法。
- **即插即用**：不修改冻结的 CLIP 编码器，仅在外围添加可学习的提示向量。
- **个性化与全局化的平衡**：客户端本地保留个性化提示，服务端聚合全局提示，通过动态聚合权重平衡两者。
- **多样性显式建模**：通过 $\mathcal{L}_{\text{div}}$ 显式鼓励多组提示捕获不同的跨模态交互模式。

---

## 三、复现方案

### 3.1 环境配置

| 组件 | 版本/规格 |
|------|----------|
| 操作系统 | Ubuntu 20.04 / Windows 11 |
| GPU | NVIDIA RTX 3090 / 4090 (24GB) 或等效 GPU |
| Python | 3.9+ |
| 深度学习框架 | PyTorch 2.0+ |
| CLIP 模型 | ViT-B/16 (openai/clip-vit-base-patch16) |
| 关键依赖 | torchvision, numpy, tqdm, wandb (可选) |

### 3.2 数据集

按原文实验设置，使用以下数据集进行 base-to-novel 泛化评估：

| 数据集 | 类别数 | Base类 | Novel类 | 样本数 | 特点 |
|--------|--------|--------|---------|--------|------|
| ImageNet | 1,000 | 500 | 500 | ~1.28M | 大规模通用数据集 |
| Caltech101 | 101 | 51 | 50 | ~9K | 通用物体识别 |
| OxfordPets | 37 | 19 | 18 | ~7K | 宠物细粒度分类 |
| StanfordCars | 196 | 98 | 98 | ~16K | 车辆细粒度分类 |
| Flowers102 | 102 | 51 | 51 | ~8K | 花卉细粒度分类 |
| Food101 | 101 | 51 | 50 | ~101K | 食物分类 |
| SUN397 | 397 | 199 | 198 | ~109K | 场景分类 |
| DTD | 47 | 24 | 23 | ~5.6K | 纹理分类 |
| EuroSAT | 10 | 5 | 5 | ~27K | 卫星图像分类 |
| UCF101 | 101 | 51 | 50 | ~13K | 动作识别 |

### 3.3 评估协议

1. **Base-to-Novel 泛化**：在 base 类上训练，评估 base 类准确率和 novel 类准确率，计算调和平均 $\text{HM} = \frac{2 \times \text{Base} \times \text{Novel}}{\text{Base} + \text{Novel}}$。

2. **综合评估指标**：$\text{CM} = \frac{\text{Local} + \text{HM}}{2}$，同时考虑本地分布精度和泛化能力。

3. **跨数据集迁移**：在 ImageNet 上训练，直接在其他 9 个数据集上测试。

4. **少样本设置**：每个类别 16 个训练样本（16-shot）。

### 3.4 复现步骤

1. **阶段一：代码框架搭建**（约 2 天）
   - 克隆 FedMGP 开源代码（若有）或从零实现
   - 搭建 CLIP ViT-B/16 加载 + 冻结逻辑
   - 实现多组文本-视觉提示参数定义

2. **阶段二：核心算法实现**（约 3 天）
   - 实现多组提示前向传播（文本特征 + 视觉特征 + 余弦相似度计算）
   - 实现多样性损失 $\mathcal{L}_{\text{div}}$
   - 实现基于客户端采样的联邦训练循环
   - 实现相似度引导的动态聚合

3. **阶段三：数据准备与评估**（约 2 天）
   - 下载并预处理 10 个数据集
   - 实现 base/novel 类别划分
   - 实现评估指标计算（Base Acc, Novel Acc, HM, CM）

4. **阶段四：基���复现实验**（约 3 天）
   - 在 10 个数据集上运行 base-to-novel 实验（16-shot）
   - 跨数据集迁移实验
   - 与原文结果对比验证复现正确性

5. **阶段五：优化与补充实验**（约 5 天）
   - 实现本文提出的优化方案
   - 运行补充实验
   - 结果分析与可视化

6. **阶段六：报告撰写**（约 3 天）
   - 撰写实验报告
   - 制作结果图表
   - 总结分析

### 3.5 复现验证标准

复现成功标准：在 ImageNet 上 Base Acc ≥ 原文 × 0.95，Novel Acc ≥ 原文 × 0.90，即 Base Acc ≥ 68.0%，Novel Acc ≥ 69.5%。

---

## 四、优化方案

### 4.1 自适应提示组数量（核心优化）

**问题**：原文将提示组数量固定为 $G=5$，但不同数据集的类别数、类间差异、图像复杂度差异很大。固定 $G$ 可能导致简单数据集算力浪费、复杂数据集多样性不足。

**方案**：引入基于客户端数据复杂度的自适应提示组数量调整机制。

**技术路线**：
1. 在每轮联邦训练前，计算客户端本地数据的平均类间特征散度（使用冻结 CLIP 编码器提取特征，计算类中心之间的平均欧氏距离 $D_{\text{inter}}$）。
2. 根据散度动态调整 $G_k$：
   $$G_k = \max(2, \min(10, \lfloor \alpha \cdot D_{\text{inter}} + \beta \rfloor))$$
   其中 $\alpha, \beta$ 为超参数。
3. 服务端聚合时，对不同 $G$ 的提示组进行对齐（按顺序匹配，多出的组取平均）。

**预期效果**：简单数据集使用更少提示组降低计算开销，复杂数据集使用更多组提升精度。

**工程性质**：中等。需要修改提示组初始化逻辑和服务端聚合逻辑。

### 4.2 提示向量量化压缩（工程优化）

**问题**：虽然 FedMGP 通信量已很小（5.1K 参数），但在跨设备联邦场景（手机、IoT 设备）中仍有优化空间。

**方案**：对提示向量进行 int8 量化传输，客户端收到后反量化使用。

**技术路线**：
1. 训练时保持 fp16/fp32 精度。
2. 上传前对提示向量进行对称量化：
   $$\mathbf{P}_{\text{int8}} = \text{round}\left(\frac{\mathbf{P}}{\max|\mathbf{P}|} \times 127\right)$$
3. 下载后反量化：
   $$\mathbf{P}_{\text{deq}} = \frac{\mathbf{P}_{\text{int8}}}{127} \times \max|\mathbf{P}|$$
4. 通信量降低至原来的 1/4（int8 vs fp32）。

**预期效果**：通信量从 5.1K × 4 bytes ≈ 20KB 降至 5.1K × 1 byte ≈ 5KB，在不显著影响精度的情况下进一步降低通信开销。

**工程性质**：较高。纯工程实现，不涉及算法创新。

### 4.3 多样性损失计算缓存优化（工程优化）

**问题**：原文 $\mathcal{L}_{\text{div}}$ 每轮需要计算 $G(G-1)/2$ 对特征之间的余弦相似度，当 $G$ 较大时计算开销不可忽略。

**方案**：缓存前一轮的特征向量，当提示参数变化较小时复用缓存减少计算。

**技术路线**：
1. 缓存上一轮的联合特征 $\{\mathbf{f}^g\}_{g=1}^G$。
2. 每轮计算参数变化量 $\Delta\mathbf{P} = \|\mathbf{P}^{(t)} - \mathbf{P}^{(t-1)}\|$。
3. 若 $\Delta\mathbf{P} < \epsilon$，使用缓存特征计算 $\mathcal{L}_{\text{div}}$；否则重新计算并更新缓存。
4. $\epsilon$ 设为 1e-4 级别。

**预期效果**：训练后期多样性趋于稳定时，节省 50-70% 的多样性损失计算时间。

---

## 五、补充实验方案

### 5.1 编码器架构泛化性验证

**动机**：原文仅在 ViT-B/16 上实验，未验证方法在其他 CLIP 编码器上的有效性。

**实验设计**：
- 在 ViT-L/14、ResNet-50 CLIP 上重复 ImageNet 和 Caltech101 的 base-to-novel 实验。
- 对比 ViT-B/16 的结果，分析编码器架构对提示学习效果的影响。

**预期发现**：较大编码器可能带来更好的 base 精度但 novel 类泛化可能饱和；ResNet 编码器的表现可能不如 ViT，验证提示学习在不同架构上的鲁棒性。

### 5.2 极端数据异质性消融

**动机**：原文仅报告了 Dirichlet α=0.5 的结果，未探索更极端的异质性场景。

**实验设计**：
- 在 CIFAR-100 上设置 Dirichlet α ∈ {0.01, 0.05, 0.1, 0.3, 0.5, 1.0, 10.0}。
- 对比 FedMGP 与基线方法（FedAvg 微调、单组提示）在各 α 下的性能。
- 分析多样性损失 $\lambda$ 在不同异质性下的最优取值。

**预期发现**：FedMGP 在极端异质性（α ≤ 0.1）下优势应更为显著；$\lambda$ 需要随异质性增大而调大以增强个性化。

### 5.3 客户端数量扩展性实验

**动机**：原文 CLIP 数据集仅用 10 个客户端，实际联邦场景可能涉及上百个客户端。

**实验设计**：
- 在 CIFAR-100 上设置客户端数 K ∈ {10, 20, 50, 100}。
- 每轮参与客户端比例固定为 30%。
- 记录收敛轮数、最终精度、总通信量。

**预期发现**：验证 FedMGP 的相似度引导聚合在大规模客户端下的扩展性，分析 top-s 选择策略的效果。

### 5.4 客户端掉线与延迟鲁棒性

**动机**：原文假设所有选中客户端都能准时完成训练并上传，实际联邦场景中存在客户端掉线或延迟。

**实验设计**：
- 在 CIFAR-100（100 客户端）上随机模拟客户端掉线，掉线率 ∈ {10%, 20%, 30%, 50%}。
- 掉线客户端不参与当前轮聚合。
- 对比 FedMGP 与 FedAvg 在各掉线率下的性能下降幅度。

**预期发现**：FedMGP 的个性化特性使其对客户端掉线有更好的鲁棒性。

### 5.5 多样性损失消融与变体探索

**动机**：原文仅验证了多样性损失的有无，未深入分析多样性的来源和形式。

**实验设计**：
1. **不同模态的多样性**：分别只对文本特征、只对视觉特征、同时计算多样性损失，对比效果差异。
2. **不同多样性度量**：对比余弦相似度（L1）、欧氏距离、KL 散度作为多样性度量。
3. **非对称多样性权重**：对不同模态的多样性设置不同权重 $\lambda_t, \lambda_v$。

**预期发现**：文本端多样性可能比视觉端多样性更重要；余弦相似度作为度量可能不是最优选择。

### 5.6 视觉提示初始化策略分析

**动机**：视觉提示的初始化方式可能影响收敛速度和最终性能，原文未进行系统分析。

**实验设计**：
- 对比四种初始化策略：
  1. 随机初始化（原文方法）
  2. 零初始化
  3. 基于预训练图像特征的 PCA 主成分初始化
  4. 用文本提示的特征作为视觉提示初始值（跨模态初始化）
- 在 ImageNet 上记录收敛曲线和最终精度。

**预期发现**：跨模态初始化可能加速早期收敛；零初始化可能导致训练不稳定。

### 5.7 与 FedProx 的协同效应

**动机**：在极端数据异质性下，FedProx 的近端约束被证明有效。FedMGP 未探索与 FedProx 的结合。

**实验设计**：
- 在极端异质性（α=0.01）下对比：
  1. 纯 FedMGP
  2. FedMGP + FedProx（在本地损失中加入近端项 $\frac{\mu}{2}\|\mathbf{P} - \mathbf{P}_{\text{global}}\|^2$）
- 调节近端系数 $\mu \in \{0.01, 0.1, 0.5, 1.0\}$。

**预期发现**：FedMGP + FedProx 在极端异质性下可能优于纯 FedMGP。

---

## 六、实验计划与时间安排

### 6.1 总体时间线

| 阶段 | 内容 | 预计时间 |
|------|------|---------|
| 第1周 | 环境搭建 + 数据准备 + 核心算法实现 | 5天 |
| 第2周 | 基线复现实验 + 与原文对比验证 | 5天 |
| 第3周 | 优化方案实现与实验 | 5天 |
| 第4周 | 补充实验运行 | 3天 |
| 第5周 | 结果分析 + 报告撰写 | 5天 |

### 6.2 实验矩阵

| 类别 | 实验 | 数据集 | 预计GPU天数 |
|------|------|--------|------------|
| 复现 | Base-to-Novel × 10数据集 | 10个CLIP数据集 | 3 |
| 复现 | 跨数据集迁移 | ImageNet→9个 | 1 |
| 优化 | 自适应提示组数量 | ImageNet, Caltech101 | 1 |
| 优化 | 量化压缩对比 | ImageNet | 0.5 |
| 补充 | 编码器泛化 | ViT-L, ResNet-CLIP | 2 |
| 补充 | 极端异质性 | CIFAR-100 | 1.5 |
| 补充 | 客户端扩展性 | CIFAR-100 | 1 |
| 补充 | 掉线鲁棒性 | CIFAR-100 | 1 |
| 补充 | 多样性损失消融 | ImageNet | 1 |
| 补充 | 视觉提示初始化 | ImageNet | 0.5 |
| 补充 | FedProx协同 | CIFAR-100 | 0.5 |
| **合计** | | | **~13 GPU天** |

### 6.3 简化策略

若时间或资源不足，按以下优先级削减实验：

1. **必做**（核心）：复现实验 + 自适应提示组数量优化 + 编码器泛化补充实验
2. **优先做**：极端异质性消融 + 多样性损失变体探索
3. **选做**：量化压缩 + 客户端扩展性 + 掉线鲁棒性 + FedProx 协同

---

## 七、预期成果

### 7.1 预期结果

1. **复现验证**：在 ImageNet 上复现 FedMGP 的 base-to-novel 结果，误差在 5% 以内。
2. **优化效果**：
   - 自适应提示组数量：在保持精度的同时，简单数据集上降低 20-30% 计算量。
   - 量化压缩：通信量降低 75%，精度损失 < 0.5%。
3. **补充发现**：
   - 编码器架构对提示学习效果的影响规律。
   - FedMGP 在极端异质性下的性能极限。
   - 多样性损失的最佳配置（模态、度量、权重）。

### 7.2 交付物

1. 完整可运行的代码仓库（含复现代码 + 优化/补充实验代码）
2. 项目实验报告（含方法描述、实验设置、结果分析、可视化图表）
3. 训练好的模型权重（提示向量）

---

## 八、关键风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 官方代码未开源 | 中 | 需从零实现 | 参考 pFedMMA 开源代码的工程框架，复现 FedMGP 算法 |
| GPU 资源不足 | 中 | 实验周期延长 | 优先使用较小数据集（DTD、EuroSAT、OxfordPets），CIFAR-100 替代 ImageNet 做消融 |
| 复现结果与原文差异大 | 低 | 优化实验基准不可靠 | 与作者邮件确认超参数细节，使用网格搜索找最佳超参数 |
| ViT-L/14 显存不足 | 中 | 无法完成编码器泛化实验 | 使用梯度检查点 + 混合精度训练；或降级为 ViT-B/32 |

---

## 附录：三篇论文详细对比

| 维度 | FedMGP | pFedMMA | FedDDA |
|------|--------|---------|--------|
| 核心组件 | 多组文本+视觉提示 | 模态特定 Adapter + 共享跨模态投影 | 文本先验解耦 + 视觉动态适配 + MoE |
| 跨模态融合方式 | 每组内文本和视觉提示协同 | Adapter 并行处理文本/视觉 | 文本"引导"视觉适配器 |
| 个性化机制 | 多样性损失 + 客户端本地提示 | 本地个性化 Adapter + 全局共享投影矩阵 | 本地提示 + 全局提示解耦 |
| 聚合策略 | 余弦相似度加权动态聚合 | 非对称聚合（局部/全局分离） | 全局提示平均聚合 |
| 通信参数 | ~5.1K | ~3.0K | 较大（含MoE参数） |
| 理论分析 | SNR定理（动态聚合最优） | 无 | 无 |
| 主要优势 | 跨模态融合强、理论完善 | 通信量最小、多领域数据集 | 文本引导视觉、MoE门控灵活 |
| 主要局限 | 提示组数固定、编码器单一 | 跨模态交互不充分 | 通信量大、方法复杂 |

从课程主题匹配度、方法清晰度、可改进空间三个维度综合考量，FedMGP 是最合适的复现基准。

---

> **文档状态**: 待导师审阅  
> **最后更新**: 2026-05-09
